const src = 'src';
const dist = 'dist';
const templates = `${src}/templates/**`;
const example = 'example/my-icons.cson';

const config = {
  src,
  dist,
  example,
  templates,
};

module.exports = config;
